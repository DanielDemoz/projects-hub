name: Demo URL health check

on:
  schedule:
    - cron: '0 12 * * 1'
  workflow_dispatch:

permissions:
  issues: write
  contents: read

jobs:
  check-demos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check live demo URLs
        id: check
        run: |
          python3 << 'PY'
          import json, urllib.request, os

          with open("projects.json") as f:
              projects = json.load(f)

          failures = []
          for p in projects:
              if p.get("status") != "live" or not p.get("demoUrl"):
                  continue
              url = p["demoUrl"]
              try:
                  req = urllib.request.Request(url, method="HEAD", headers={"User-Agent": "projects-hub-health-check"})
                  with urllib.request.urlopen(req, timeout=20) as resp:
                      code = resp.status
                  if code >= 400:
                      failures.append(f"- **{p['name']}** — HTTP {code} — {url}")
              except Exception as e:
                  failures.append(f"- **{p['name']}** — {e} — {url}")

          with open(os.environ["GITHUB_OUTPUT"], "a") as fh:
              if failures:
                  fh.write("failed=true\n")
                  fh.write("body<<EOF\n")
                  fh.write("## Live demo health check failed\n\n" + "\n".join(failures) + "\n")
                  fh.write("EOF\n")
              else:
                  fh.write("failed=false\n")
          PY

      - name: Open issue on failure
        if: steps.check.outputs.failed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const title = 'Demo health check: live URL(s) unreachable';
            const body = `${{ steps.check.outputs.body }}\n\n_Auto-generated by weekly demo health check._`;
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'demo-health'
            });
            if (issues.some(i => i.title === title)) return;
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body,
              labels: ['demo-health']
            });
